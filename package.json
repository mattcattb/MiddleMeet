{
  "name": "matty-stack",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:server": "bun run --filter @matty-stack/server dev",
    "dev:web": "bun run --filter @matty-stack/web dev",
    "build": "bun run --filter '*' build",
    "db:generate": "bun run --filter @matty-stack/server db:generate",
    "db:migrate": "bun run --filter @matty-stack/server db:migrate",
    "db:studio": "bun run --filter @matty-stack/server db:studio"
  }
}
